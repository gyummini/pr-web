# 김경민 PR 웹사이트 — 개발 명세서

> 이 문서는 Claude Code 프로젝트의 CLAUDE.md로 사용한다.
> 모든 구현 판단은 이 문서를 기준으로 하고, 문서와 어긋나는 구현이 필요하면 진행 전에 사용자에게 확인한다.

---

## 0. 프로젝트 개요

- **목적**: 게임 기획 직무 취업용 통합 PR 웹사이트. 자기소개서(진술)와 포트폴리오 문서(증거)를 연결해 보여준다.
- **컨셉**: 추리/수사 게임. 방문자(채용 담당자)는 '탐정', 자기소개서는 '진술', 포트폴리오는 '증거', 사이트 전체는 '사건 파일'. 조수 캐릭터 **클루(Clue)** 1인이 안내역 (설정: 캐릭터_조수_클루.md 참조).
- **타겟 사용자**: 채용 담당자. **1인당 체류 시간 2~5분을 전제**로 모든 설계를 판단한다.
- **레퍼런스 감성**: 모바일 서브컬처 게임의 튜토리얼/대화창 UX.

### 절대 원칙 (충돌 시 이 순서로 우선)
1. **정보 접근은 항상 즉시**: 어떤 화면에서든 2클릭 이내에 모든 포트폴리오 문서에 도달할 수 있어야 한다.
2. **게임적 경험은 항상 선택**: 연출·수집·히든은 여유 있는 방문자를 위한 것. 바쁜 방문자를 막는 벽이 되어서는 안 된다.
3. **읽기를 방해하지 않는다**: 본문 열람 중 페이지 강제 이동, 강제 대기, 스킵 불가 연출 금지.

---

## 1. 정보 구조

상단 고정 탭 3개. 모든 페이지에서 항상 노출되며 자유 이동 가능.

| 탭 | 내용 | 컨셉 명칭 |
|---|---|---|
| 기본 사항 | 이력서 (인적사항, 학력, 스킬, 연락처) | 인물 신상 조서 |
| 세부 사항 | 자기소개서 4챕터 + 증거 앵커 | 진술 기록 |
| 수집된 증거 | 증거 카드 7장 + 히든 진행도 | 증거 보관함 |

수집된 증거 탭에는 **수집 카운트 뱃지**(예: 4/7)를 상시 표시한다.

---

## 2. 화면 명세

### 2-1. 시작 페이지
- 클루 스탠딩 + 하단 대화창. 대사는 **스크립트_인트로.json**을 사용한다 (컨셉 안내 5줄 + 선택지).
- 클루는 방문자를 **"탐정님"**이라고 부른다.
- **SKIP 버튼: 화면 우상단, 첫 프레임부터 상시 노출.**
- 대사 완료 또는 SKIP 시 **선택지 창** 표시 (2개):
  - **[직접 수사한다]** (부가 설명: 약 5분) → 세부 사항(CASE 01)으로 이동. 일반 진행.
  - **[수사 결과만 본다]** (부가 설명: 바로 보기) → **모든 증거(히든 포함)가 해금된 상태**의 수집된 증거 페이지로 이동.
- 선택지 문구는 컨셉 언어를 유지하되 부가 설명으로 기능을 명확히.
- 재방문 처리: 세션 내에서 시작 페이지 재진입 시 대사 자동 스킵(선택지만 표시).

### 2-2. 기본 사항 페이지
- 이력서 내용을 파일철/조서 스타일로 표시.
- 정적 페이지. 인터랙션 없음. 연락처, 이력서 PDF 다운로드 버튼 포함.

### 2-3. 세부 사항 페이지 (자기소개서)
- 파일철 느낌의 레이아웃. 우측(또는 상단)에 챕터 탭/인덱스:
  - **CASE 01. 인물 분석** — 성격의 장점
  - **CASE 02. 사건과 교훈** — 단점과 개선을 위한 노력
  - **CASE 03. 핵심 증거** — 직무 역량
  - **CASE 04. 동기, 그리고 결말** — 지원동기 및 입사 후 포부
- 본문은 세로 스크롤로 읽는다. 대사창 형식 아님.
- 본문 중 앵커 문장은 **하이라이트 + 증거 아이콘**으로 표시.

#### 앵커(증거) 팝업
- 하이라이트 클릭 시 팝업(모달) 표시:
  - 클루 **SD 모습** + 증거별 대사. 대사는 **콘텐츠_증거카드.json**의 `sd_dialogue`(재클릭 시 `sd_dialogue_revisit`, null이면 sd_dialogue 재사용)를 사용한다.
  - 해당 포트폴리오의 **한 줄~세 줄 요약**
  - **[문서 열기]** 버튼 → 외부 링크(드라이브/노션) **새 탭**으로 열기
- 팝업 닫기 시:
  - **페이지 이동 없음.** 읽던 위치 유지.
  - 증거 카드가 축소되며 '수집된 증거' 탭으로 날아가 흡수되는 **수집 애니메이션** 재생 + 탭 뱃지 카운트 증가.

#### 증거 수집 규칙
- 클릭한 앵커 → 즉시 수집 (팝업 + 애니메이션).
- **챕터 이탈 시** (다른 챕터로 이동하거나 다른 탭으로 이동), 해당 챕터에서 클릭하지 않은 증거는 **자동 일괄 수집** (연속 수집 애니메이션 재생).
- 자동 수집 트리거는 **"열람한 챕터에서 나가기"로 한정**. 열지 않은 챕터의 증거는 수집되지 않는다.
- **예외**: 마지막 챕터(CASE 04)의 증거는 '수집된 증거' 페이지로 이동하는 시점에 자동 수집한다.
- 한 챕터의 증거를 모두 수집하면 **스팀 도전과제 스타일의 토스트 알림** 표시 (예: "CASE 01. 완벽 수집!"). 화면 구석, 자동 소멸, 클릭 요구 없음.

### 2-4. 수집된 증거 페이지
- 증거 카드 목록. 수집된 증거는 제목+썸네일 노출, 클릭 시 해당 포트폴리오 외부 링크로 이동(새 탭).
- **미수집 증거도 목록에 표시하고 클릭 가능** ("미확인 증거" 스타일로 구분):
  - 클릭 시 → 해당 증거가 처음 등장하는 **자기소개서 챕터 + 해당 포폴 요약**을 보여주는 화면으로 이동. 방문자가 요약만 볼지, 문서 세부를 열지 선택하게 한다.
- 목록 최하단에 **히든 증거** 슬롯:
  - 조건과 진행도 상시 노출: "조건: 모든 증거를 수집하세요! 현재: n / 7"
  - 미해금 상태에서는 클릭 불가(또는 조건 안내만).

### 2-5. 히든 해금 & 엔딩
- 7/7 달성 시 히든 증거 슬롯에 **강조 연출**(하이라이트/애니메이션).
- 클릭 시 → 클루와의 **엔딩 대화** 진행 (대화창 화면, 시작 페이지와 동일한 대사 엔진 재사용, 대사는 **스크립트_엔딩.json**) → 대화 종료 후 최종 포트폴리오(**이 웹사이트의 제작기**) 확인 가능.
- '수사 결과만 본다'를 선택한 방문자에게는 히든이 처음부터 해금 상태로 목록에 존재한다. 이 경우 엔딩 대화 없이 제작기로 바로 접근 가능 (해금 연출은 직접 수사 완주자 전용 보상).
- 엔딩 화면에 연락처 / 이력서 다운로드 버튼 재노출.

---

## 3. 증거 데이터 (7장)

| ID | 증거명 | 등장 챕터 | 외부 링크 | 비고 |
|---|---|---|---|---|
| E1 | 서브컬처 캐릭터 매력 분석서 | CASE 04 | (드라이브 링크 — 추후 삽입) | |
| E2 | 블루아카이브 Cost 시스템 역기획서 | CASE 03 | (드라이브 링크) | 팝업에 "첨부: 스킬 데이터테이블(xlsx)" 링크 병기 |
| E3 | 로블록스 브레인롯 강화 개발회고 | CASE 01, CASE 03(×2) | (드라이브 링크) | **여러 챕터 재등장.** 첫 수집 이후 재등장 앵커 클릭 시 "이 증거, 전에도 봤죠?" 류의 재등장 전용 대사. 수집 카운트는 1로 유지 |
| E4 | 에픽세븐 오토마톤 타워 개선 제안서 | CASE 03 | (드라이브 링크) | |
| E5 | AI 문서 작성 효율화 보고서 | CASE 03 | (노션 링크) | |
| E6 | 아르카나 세트 시스템 제안서 | CASE 04 | (드라이브 링크 — 범용화 후 업로드 예정) | |
| E7 | 웹사이트 제작기 (히든) | 엔딩 해금 | (추후 작성) | 조건: E1~E6 전부 수집 |

- 자기소개서 본문 텍스트와 앵커 위치는 별도 문서 「자기소개서_최종본.md」를 따른다 (굵은 문장 = 앵커).
- 증거별 요약·대사는 **콘텐츠_증거카드.json**으로 제공됨(확정). **본문·대사·요약은 코드에 하드코딩하지 말고 데이터 파일로 분리한다.**

---

## 4. 대사 엔진 명세

시작 페이지, 엔딩 대화, 앵커 팝업의 SD 대사에 공통 사용하는 자체 미니 대사 엔진을 만든다. 게임 엔진(렌파이 등) 사용 금지 — 순수 웹으로 구현하되, 아래의 '손맛'을 재현한다.

### 스크립트 포맷 (JSON, 데이터 파일 분리)
```json
[
  { "speaker": "클루", "sprite": "normal", "text": "어서 오세요, 탐정님." },
  { "speaker": "클루", "sprite": "surprised", "text": "설마 벌써 증거를 다 모으신 건가요?" },
  { "type": "choice", "options": [
    { "label": "직접 수사한다", "sub": "약 5분", "goto": "case01" },
    { "label": "수사 결과만 본다", "sub": "바로 보기", "goto": "evidence_all_unlocked" }
  ]}
]
```
- speaker(화자명), sprite(스탠딩 표정 키), text(대사), choice(선택지) 지원.
- 대사 수정은 JSON만 고치면 되도록 렌더러와 완전 분리.

### 렌파이 손맛 체크리스트 (전 항목 필수 구현)
- [ ] 텍스트 타이핑 이펙트 (글자 단위 순차 출력)
- [ ] **2단계 클릭**: 타이핑 중 클릭 → 문장 즉시 완성 / 완성 상태에서 클릭 → 다음 대사
- [ ] 대사 완료 시 대사창 우하단에 **깜빡이는 진행 표시(CTC 아이콘)**
- [ ] 스탠딩 교체 시 하드 컷 금지 — 짧은 디졸브/페이드 (0.2~0.3s)
- [ ] 대사창 등장/퇴장 페이드
- [ ] 키보드 진행 지원 (Space / Enter = 클릭과 동일)
- [ ] 연타 방지: 문장 완성 직후 극소 딜레이 (~100ms) 후 다음 진행 허용
- [ ] 화자명 표시 영역 (대사창 좌상단 네임태그)

---

## 5. 상태 관리

- 필요한 상태는 두 가지뿐: **챕터별 열람 여부**(4개), **증거별 수집 여부**(7개).
- 세션 내 메모리(JS 변수/React state)로 관리. **localStorage / sessionStorage 사용 금지.**
- 새로고침 시 초기화되어도 무방 (한 세션 내 완주 분량).
- '수사 결과만 본다' 선택 시 전 증거 수집 상태로 세팅.

---

## 6. 기술 요구사항

- **순수 웹 단일 페이지 애플리케이션.** 정적 호스팅 가능해야 함 (GitHub Pages / Vercel 등).
- 데스크톱 우선 (심사자는 회사 PC 사용 전제). 모바일은 레이아웃이 깨지지 않는 수준의 기본 대응.
- 첫 화면 로딩 3초 이내 목표. 이미지(스탠딩, SD, 썸네일)는 지연 로딩.
- 외부 문서 링크는 전부 **새 탭** (`target="_blank"`). 사이트 이탈 방지.
- 콘텐츠(자소서 본문, 대사, 증거 요약)는 데이터 파일(JSON/MD)로 분리. 코드 수정 없이 내용 교체 가능해야 함.
- 브라우저 뒤로가기가 탭/챕터 이동과 자연스럽게 동작하도록 URL 해시 또는 라우팅 처리.

---

## 7. 검수 체크리스트 (구현 완료 판정 기준)

### 안전망
- [ ] 어느 화면에서든 2클릭 이내에 모든 외부 문서 도달 가능
- [ ] 시작 페이지 SKIP이 첫 프레임부터 노출
- [ ] '수사 결과만 본다' 경로로 30초 내 전 문서 목록 도달 가능
- [ ] 미수집 증거 클릭 시 등장 챕터+요약 화면으로 정상 이동

### 수집 시스템
- [ ] 앵커 클릭 → 팝업 → 닫기 시 페이지 이동 없이 수집 애니메이션
- [ ] 챕터 이탈 시 미클릭 증거 자동 일괄 수집 (열람한 챕터만)
- [ ] CASE 04 증거는 수집된 증거 페이지 진입 시 자동 수집
- [ ] 챕터 완벽 수집 토스트 정상 표시
- [ ] E3(로블록스) 재등장 앵커가 중복 카운트되지 않음
- [ ] 7/7 달성 시 히든 강조 연출 + 엔딩 대화 진입 가능

### 대사 엔진
- [ ] 손맛 체크리스트 8항목 전부 통과
- [ ] JSON 수정만으로 대사 교체 가능

---

## 8. 인수 파일 목록 및 미확정 항목

### 함께 제공되는 파일 (전부 확정본)
| 파일 | 역할 |
|---|---|
| PR웹사이트_개발명세서.md | 본 문서. CLAUDE.md로 사용 |
| 콘텐츠_자기소개서.md | 세부 사항 페이지 본문 소스. `[CASE##]` 챕터 구분 + `{{E번호:문장}}` 앵커 문법 |
| 콘텐츠_증거카드.json | 증거 7장 메타데이터 (제목, 요약, 등장 챕터, 첨부, 클루 대사, 히든 조건) |
| 스크립트_인트로.json | 시작 페이지 대사 + 선택지 + SKIP 동작 |
| 스크립트_엔딩.json | 히든 해금 시 엔딩 대화 + 종료 후 동작 |
| 캐릭터_조수_클루.md | 클루 설정·스프라이트 명세·톤 가이드 (대사 추가 작성 시 이 톤 가이드 준수) |

### 미확정 — 플레이스홀더로 진행할 것
- **클루 일러스트 (스탠딩/SD, 표정 4종)**: 캐릭터_조수_클루.md 5번 명세대로 추후 제공. 그 전까지 sprite 키별 플레이스홀더 이미지 사용.
- **외부 문서 링크 URL**: 콘텐츠_증거카드.json의 PLACEHOLDER_* 값. 상수/데이터 파일 한 곳에서만 참조하도록 구현.
- **제작기(E7) 본문**: 사이트 완성 후 작성. 링크 슬롯만 준비.
- **비주얼 디자인 토큰(색, 폰트, 파일철 그래픽)**: 별도 디자인 가이드 예정. 1차 구현은 와이어프레임 수준의 무채색+포인트 컬러로.

### 구현 시 임의 판단 금지 항목
- 대사·요약·본문 텍스트를 새로 창작하거나 수정하지 않는다. 제공된 데이터 파일이 유일한 소스.
- 증거 수(7), 챕터 수(4), 수집 규칙을 변경하지 않는다.
