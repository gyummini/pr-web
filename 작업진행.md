# 작업 진행 기록 — PR_web

> **새 세션은 이 문서를 먼저 읽고 시작한다.**
> 최종 갱신: 2026-08-26 / 기준 커밋: `aad733c` 이후 / 작업 트리 clean
>
> 판단 기준이 충돌하면 **`PR웹사이트_개발명세서.md`가 항상 우선**한다. 이 문서는 그 명세서를
> 대체하지 않고, 명세서에 안 적힌 "그동안 무슨 일이 있었는지"를 채운다.
>
> ### ✱ 커밋 규칙
>
> **커밋할 때마다 이 문서의 5장(작업 이력)에 항목을 추가한다.** 커밋과 같은 스테이지에 올려
> 함께 커밋한다. `.githooks/pre-commit`이 이 문서가 빠진 커밋을 막는다.
>
> 한 항목에 들어갈 것: **무엇을 왜 바꿨는지 / 새로 생긴 파일 / 발견해서 고친 버그 /
> 다음 세션이 몰라서 헤맬 만한 것.** diff를 다시 읽으면 알 수 있는 내용은 적지 않는다.

---

## 0. 읽는 순서

1. `PR웹사이트_개발명세서.md` — CLAUDE.md 역할의 유일한 기준 문서. 절대 원칙·화면 명세·검수 기준.
2. 이 문서 — 작업 이력, 현재 상태, 함정, 남은 일.
3. `캐릭터_조수_클루.md` — 클루 캐릭터 설정·말투 기준.
4. 필요 시 `작업지시_*.md` — 개별 기능의 원본 지시서.

---

## 1. 프로젝트 정체

김경민(게임 기획 직무 지원자)의 **취업용 PR 웹사이트**. 자기소개서를 '진술', 포트폴리오 문서를 '증거'로
치환한 **추리/수사 게임 컨셉**이며, 조수 캐릭터 **클루(Clue)** 가 안내역이다.

- 타겟: 채용 담당자. **체류 시간 2~5분 전제**로 모든 설계를 판단한다.
- 배포: <https://pr-web-one.vercel.app> — `noindex, nofollow` 적용.
- 저장소: `github.com/gyummini/pr-web` (비공개). `main` 푸시 → Vercel 자동 재배포.

### 절대 원칙 (충돌 시 이 순서)

1. **정보 접근은 항상 즉시** — 어떤 화면에서든 2클릭 이내에 모든 포트폴리오 문서에 도달.
2. **게임적 경험은 항상 선택** — 연출·수집·히든이 바쁜 방문자를 막는 벽이 되면 안 된다.
3. **읽기를 방해하지 않는다** — 강제 이동, 강제 대기, 스킵 불가 연출 금지.

---

## 2. 기술 스택과 실행

- **빌드 도구 없는 순수 정적 SPA.** ES 모듈 + History API 경로 라우팅. npm 빌드 단계 없음.
- 콘텐츠는 루트의 **한글 파일명 JSON/MD를 런타임 fetch** — 데이터 파일만 고치면 화면에 반영된다.
  **텍스트를 JS에 하드코딩하지 말 것.**
- 상태는 **세션 메모리만** 사용. 게임 상태(챕터 열람·증거 수집)에 `localStorage` 금지(명세 요구).
  예외: 계측 전용 `sessionStorage['prweb.nav']` — 진행도만 담고 개인정보는 담지 않는다.

### 로컬 실행

```
node .claude/spa-server.mjs . 8123
```

`.claude/launch.json`에 `pr-web` 구성으로 등록되어 있다 (포트 8123).
SPA fallback이 필요해 단순 `python -m http.server`로는 경로 라우팅이 404가 난다 — 위 서버를 쓸 것.

### 배포

`vercel.json`의 rewrite가 모든 경로를 `index.html`로 넘긴다. `.vercelignore`가 내부 문서를 배포에서 제외한다.
**내부 문서를 새로 추가하면 반드시 `.vercelignore`에 등록할 것.**

---

## 3. 파일 지도

### 코드

| 경로 | 역할 |
|---|---|
| `index.html` | 셸. 고정 탭 3개, OG 메타, Vercel Analytics 주입 스크립트(+큐 스텁) |
| `assets/js/main.js` | 부트스트랩. 데이터 로드 → 라우터 시작 → 계측 초기화 |
| `assets/js/router.js` | 경로 라우팅, `navigate()`, `onRouteChange()`, 레거시 `#/` 1회 리다이렉트 |
| `assets/js/data.js` | 콘텐츠 fetch + MD 파서(자기소개서/제작기). 앵커 문법 `{{E번호:문장}}` 처리 |
| `assets/js/state.js` | 세션 상태(열람 챕터·수집 증거·토스트 중복 방지). `BASE_EVIDENCE_IDS`, `baseUnlocked()` |
| `assets/js/collect.js` | 증거 수집 규칙, 챕터 이탈 시 자동 일괄 수집 |
| `assets/js/ui.js` | 뱃지, 수집 비행 애니메이션, 토스트, 외부 문서 열기 |
| `assets/js/sprites.js` | 클루 표정 4종 레이어 크로스페이드 |
| `assets/js/preload.js` | 유휴 시간 사전 로딩, 수첩 폰트 선로딩 |
| `assets/js/engine.js` | 대사 엔진 (렌파이식 진행) |
| `assets/js/popup.js` | 증거 팝업. 요약 메모 렌더 + 액션(증거 수집 / 더 자세히 살펴보기) |
| `assets/js/memo.js` | 증거 요약 메모 프레임워크. 뼈대 고정 + `block.kind`별 렌더러 |
| `assets/js/views/brief.js` | Interactive Brief 껍데기. `hasBrief()`가 동선 분기의 단일 기준 |
| `assets/js/briefs/flow.js` | E2 브리프 인터랙션 (플로우 차트 + 마스터/상태 데이터) |
| `assets/js/figures.js` | 구 제작기 페이지 시각 자료 렌더 (슬롯 ID별) |
| `assets/js/analytics.js` | Vercel 커스텀 이벤트 계측. 라우터를 구독만 하고 라우팅 코드와 섞이지 않음 |
| `assets/js/views/*.js` | 화면별 렌더러 (intro/basic/dossier/evidence/detail/ending/notebook/docs/making/notfound/brief) |

### 콘텐츠 데이터 (수정하면 사이트에 바로 반영)

| 파일 | 내용 |
|---|---|
| `콘텐츠_자기소개서.md` | CASE01~04 + EPILOGUE. `## [CASE01] 컨셉 — 부제` 형식, 본문에 `{{E3:문장}}` 앵커 |
| `콘텐츠_증거카드.json` | 증거 7장(E1~E7). `doc_type`/`thumb`/`title`/`subtitle`/`chapters`/`summary`/`url`/`attachments`/`sd_dialogue` |
| `콘텐츠_수사수첩.json` | 히든(E7) 수첩 본문. `cover`/`part1`/`part2`/`part3`/`folded` |
| `스크립트_인트로.json`, `스크립트_엔딩.json` | 대사 스크립트 |
| `data/resume.json` | 기본 사항 페이지 데이터. `projects`/`skills`는 **비면 렌더링 안 함 — 임의로 채우지 말 것** |
| `콘텐츠_제작기.md` | 구 제작기 페이지 원고 (아래 6장 참조 — 고아 상태) |

### 증거 7장

| ID | 유형 | 제목 | 등장 |
|---|---|---|---|
| E1 | 캐릭터 분석서 | 서브컬처 캐릭터 매력 분석서 | CASE03 |
| E2 | 시스템 역기획서 | 블루아카이브 Cost 시스템 역기획서 | CASE02 (첨부 1) |
| E3 | 개발 회고 | 로블록스 브레인롯 강화 개발회고 | CASE01 (첨부 1) |
| E4 | 개선 제안서 | 에픽세븐 오토마톤 타워 개선 제안서 | CASE02 |
| E5 | 시스템 기획서 | 아르카나 세트 시스템 제안서 | CASE03 |
| E6 | 활용 보고서 | AI 문서 작성 효율화 보고서 | CASE04 |
| E7 | (히든) | 클루의 수사 수첩 | E1~E6 전부 수집 시 해금 |

### 배포 제외 (내부 문서)

`PR웹사이트_개발명세서.md`, `캐릭터_조수_클루.md`, `히든포폴_제작기_초안.md`,
`히든_클루의수사수첩_초안.md`, `작업지시_수첩디자인이식.md`, `작업지시_대사톤개편.md`,
`design/`, `.claude/`, `.gitignore`, 그리고 **이 문서**.

---

## 4. 화면 구성

| 경로 | 화면 | 비고 |
|---|---|---|
| `/intro` | 시작 — 클루 대화 | 첫 진입 시 사건 파일 표지 오프닝 트랜지션(≤1.2초, 아무 곳이나 클릭 시 스킵) |
| `/basic` | 기본 사항 = 인물 신상 조서 | 인적사항·학력·프로젝트·보유 기술·경력·PDF 다운로드 |
| `/case/01` ~ `/case/04`, `/case/epilogue` | 세부 사항 = 진술 기록 | 자기소개서 본문 + 증거 앵커(형광펜 + 🔍) |
| `/evidence` | 수집된 증거 = 증거 보관함 | 히든 슬롯 + 카드 그리드(데스크톱 3열) |
| `/evidence/:id` | 증거 상세 | 등장 챕터 + 요약 |
| `/ending` | 엔딩 대화 | |
| `/notebook` | **히든 E7 — 클루의 수사 수첩** | 6쪽 버튼식 페이지네이션(cover / part1 / part2 前·後 / part3 / folded) |
| `/docs` | **컨셉 없는 문서 목록** | 지원서에 붙이는 2번 링크. 아래 참조 |
| `/making` | 구 제작기 | **고아 라우트** — 아래 6장 참조 |

### `/docs`의 원칙 (중요)

이 페이지를 여는 사람은 **게임형 열람을 원하지 않는 검토자**다.
수사 컨셉 어휘·클루·수집 UI·도장·진행도가 **한 개도 보이면 안 된다.** 상단 탭 바도 숨긴다.
데이터는 `콘텐츠_증거카드.json`을 그대로 쓰되(복제 금지), `sd_dialogue`·`chapters`·E7은 제외하고
증거 코드 대신 단순 순번을 쓴다. 이 경로에서는 컨셉 전용 에셋을 사전 로딩하지 않는다.

---

## 5. 작업 이력 (대화 기록 복원)

### 7/07 ~ 7/09 — 1차 구현과 배포
명세서 + 이력서 PDF 기반 구현. 클루 일러스트(투명 배경 확정본) 적용 — 스탠딩 4종은 union bbox
공통 크롭 311×1100으로 표정 전환 위치를 고정, SD는 1종(529×512)으로 통합.
E1~E6 외부 링크 + E3 로블록스 플레이 링크 연결. GitHub + Vercel 배포(사용자가 직접 수행).
이력서·자기소개서 PDF 교체(수정본으로 두 번 갱신).

### 7/09 ~ 7/10 — 히든 포폴 1차: 제작기 페이지
`히든포폴_제작기_초안.md` 기반으로 `/making` 구현. 시각 자료 10/10 완료.
도식·표 계열은 이미지 대신 **CSS 컴포넌트로 직접 구현**(브라우저 CDP 캡처가 불안정했던 것도 이유).
캐릭터 비포 이미지는 "프롬프트를 세세히 넣지 않았을 때의 결과물"이라는 맥락으로 사용.

### 7/11 ~ 7/12 — 히든 포폴 2차: 수사 수첩으로 교체
`작업지시_수첩디자인이식.md` 기준으로 E7을 **'클루의 수사 수첩'(`/notebook`)** 으로 교체.
페이지 넘김은 처음에 휠 임계값 방식으로 만들었다가, 사용자 판단으로 **버튼식 페이지네이션**으로 최종 변경
(하단 중앙 ◀ n/6 ▶ + 키보드, 전환 잠금, 첫/끝 비활성). 버튼 위치도 하단 → 좌우 중앙으로 조정.
넘김 애니메이션(CSS `rotateY`)은 **사용자 지시로 reduced-motion 예외 없이 모든 환경에서 재생**한다
(개발 머신이 Windows 애니메이션 끄기 상태라 안 보이던 문제 때문). 대화 씬 배경 `bg_office.jpg` 추가.
해상도별 클루 크기 편차(2K에서 너무 작음) 수정.

### 7/25 ~ 7/26 — 증거 페이지 개편 + 대사 톤 개편 + 전환 끊김 개선
- 증거 페이지: 세로 1열 리스트 → **카드 그리드**, 실물 표지 썸네일(`thumb_e1~e6`), `doc_type` 라벨,
  정보 위계 재정렬(제목 우선 / 증거 코드는 보조), "증거 보관함" 도장 + 실제어 병기.
  미수집 카드는 블러 + 채도 저하하되 **클릭은 계속 가능**.
- 형광펜이 두 줄로 넘어갈 때 윗줄이 빠지는 버그 수정.
- 대사 톤 개편(`작업지시_대사톤개편.md`): 기능 설명체·설명서 어휘 제거, 수첩 내지 독백체로.
- 전환 끊김 개선: **스탠딩을 4종 전부 DOM에 겹쳐 두고 `opacity`만 전환**(`src` 교체 금지 — 디코딩
  중 빈 프레임이 반드시 생긴다), 수첩 폰트 선로딩(증거 5/7 도달 시점에 백그라운드 요청),
  인트로 대사 재생 중 `requestIdleCallback`으로 유휴 사전 로딩.

### 7/27 ~ 7/28 — 수첩 콘텐츠 완성 + 기본 사항 보강
증거 카드에 첨부 링크 칩 추가(E2 데이터테이블 xlsx, E3 플레이 링크).
수첩 2부 관찰 메모에 이미지 5종(`memo_1~5`), 3부 현장 사진 2장(`notebook_photo_1~2`) 연결.
썸네일 위 📄 폴백 아이콘 겹침 제거. 손글씨 폰트를 **교보 손글씨 2025**로 교체(가독성).
기본 사항 페이지에 **프로젝트·보유 기술 섹션 신설** — 로블록스 출시 2026.07, 팀 프로젝트 2021~2023(팀장,
3건 묶음), DB 설계·관리 2021. **시간순 오름차순.** 보유 기술은 Python / C·C++ / Lua(Roblox) /
데이터 테이블(Excel) / AI 활용. **과장 표현 금지** — 도구명과 실제 사용 범위만.

### 7/28 ~ 7/29 — `/docs` 신설 + PDF 재조판
컨셉 없는 문서 목록 페이지 `/docs` 신설. 다운로드 PDF를 **사이트 데이터 기반의 컨셉 없는 버전**으로 교체.
자기소개서 개정본 반영 + **문장 단위 줄바꿈**(`.` 뒤 개행 — 세부 사항 페이지에도 동일 적용).
PDF 조판 개선(장별 페이지 분리, 문단 간격·행간 확대). 인트로 선택지 제거 → 단일 동선으로 정리.

### 7/30 ~ 7/31 — 오프닝 트랜지션 + 애널리틱스
첫 진입 사건 파일 표지 오프닝 트랜지션(≤1.2초, 새 에셋 0건, CSS만, 클릭 스킵, `/docs` 및 인트로 외
경로에서는 재생 안 함). OG 이미지 `og_cover.png` 동반 제작.
"case close 연출"도 검토했으나 **사용자가 넣지 않기로 결정** — 되살리지 말 것.
`@vercel/analytics` 도입, 주입 스크립트 추가.

### 8/03 ~ 8/05 — 해시 라우팅 → 경로 라우팅 전환
Vercel Analytics가 해시 변경을 페이지뷰로 집계하지 못하는 문제 때문에 `#/` → 실제 경로로 전환.
`vercel.json` SPA rewrite 추가, 404 라우트 추가, 기존 `#/` 공유 링크는 진입 시 1회 리다이렉트.
커스텀 이벤트 3종 구현 — `page_exit`(체류 시간, **버킷 문자열**), `scroll_depth`(25/50/75/100 각 1회),
`session_progress`(세션 내 탐색 순번 + 진입 경로). `beforeunload` 미사용, 탭이 가려진 동안은 미누적.

### 8/04 — 주소 갱신
`resume.json`의 `address`를 이력서 원본과 동일한 전체 주소로 변경(이전에는 시·군 단위까지만 노출했다).
`_readme` 필드에 그 경위가 적혀 있다.

### 8/13 — 대화 기록 없음, 커밋으로만 확인됨
이 날의 두 커밋은 세션 기록이 남아 있지 않다. diff로 확인한 내용은 다음과 같다.

- **`aefc592` 자기소개서 콘텐츠와 증거 탐색 구조 개편**
  - 자기소개서에 **EPILOGUE 챕터 추가** — 파서·라우터·상태·인덱스가 `CASE\d+|EPILOGUE`를 함께 처리.
    경로는 `/case/epilogue`, 라벨은 `EPILOGUE`.
  - 앵커의 🔍 아이콘이 혼자 다음 줄로 밀려 밑줄이 어긋나던 문제 → 마지막 단어와 한 덩어리로 묶음
    (`anchorContents()` / `.anchor-tail`).
  - `collectCase04` → `collectFinalChapter`로 개명(의미 명확화). 동작은 동일.
  - 증거 카드 JSON·자기소개서 MD·자기소개서 PDF 갱신.
- **`126b38c` Update portfolio access and profile details**
  - E1~E6을 다 모은 순간 **고정 토스트**("마지막 파일이 해금되었습니다" + 수첩 바로가기). 직접 닫기 전까지 유지.
  - 히든 슬롯을 잠금 상태에서도 정보를 보여주도록 개편 — `BONUS FILE` 키커, 제목·부제 노출,
    **"남은 증거 찾기"** 버튼(미수집 첫 증거로 이동)과 **"수첩 바로 열람"** 버튼 추가.
    → 절대 원칙 2(게임적 경험은 선택)를 히든에도 적용한 변경으로 읽힌다.
  - 히든 슬롯을 그리드 **아래에서 위로** 이동.
  - 진행도 계산을 `collected.size`(E7 포함, 7 기준) → `collectedBaseCount()`(E1~E6, **6 기준**)으로 수정.


### 8/26 — 증거 요약 메모 + E2 Interactive Brief (`c52325b`, `bbda7da`)

증거 열람을 **발견 → 이해 → 조사 → 원본** 네 단계로 재구성했다. 사용자가 준 원본 문서
(zip의 PDF·docx 5종 + 노션 1종 + 데이터테이블 xlsx)를 읽고 그 내용으로 채웠다.

**증거 요약 메모** (`memo.js`) — 팝업의 요약문을 클루가 남긴 메모로 교체.
- 뼈대는 여섯 증거가 공유한다: `관찰 → 판단 → [블록] → 세 번째 칸`.
  **앞 두 칸은 고정**하고 세 번째 칸만 문서 성격을 따라 변주한다(적용/재구성/결과/제안/설계/정리).
- 가운데 블록만 증거마다 다르다 — `cycle`(E1) `mapping`(E2) `reduce`(E3) `bridge`(E4)
  `graft`(E5) `split`(E6). 블록을 추가하려면 `BLOCKS`에 kind를 넣고 데이터를 채우면 된다.
- 시각 언어는 수사 수첩에서 가져왔지만 **손글씨 폰트는 쓰지 않는다** — 팝업 시점엔 아직
  선로딩 전이고(ui.js: 5수집), 손글씨는 히든의 특별함으로 남긴다.

**E2 Interactive Brief** (`views/brief.js` + `briefs/flow.js`) — `/evidence/:id/brief`.
- 역기획서의 최종 플로우 차트를 옮기고, 스킬을 누르면 토큰이 그 위를 지나며 데이터가 바뀐다.
- 클릭 한 번이 **두 단계**로 갈린다: 마스터 데이터를 FK로 훑어 툴팁을 조립하고
  (`Student_Master → Skill_Master → Skill_Level_Detail → Skill_String`), 이어서 상태
  데이터(전투 환경 설정·런타임 상태·덱 큐)를 쓴다.
- 치환 규칙: **템플릿이 `{PARAM}%` 형태일 때만 ×100.** 문자열이 스스로 단위를 선언하므로
  파라미터 이름 목록을 따로 두지 않는다. (`ATK_UP_RATE 1.05 → 105%`, `DURATION 13 → 13`)
- 수치는 전부 원본 값이다. 회복력 합계 4857 · 배율 1.202 · 회복속도 0.5838/sec · 시작 Cost 2.48.
- 화면만은 사이트의 종이 톤을 따르지 않는다. **팔레트를 문서 범례에서 가져왔다** —
  조건=초록, 예외=노랑, 데이터 테이블=하늘, 상태 데이터=분홍. PC 전제(공지 표시).
- 흐름 속도는 반복될수록 빨라진다(620ms → 100ms, 5회). 값은 전부 데이터에 있다.

**동선 변경**
- 팝업에서 원본 문서·플레이 링크를 뺐다. 그 자리는 **브리프의 끝**이다.
- 증거 카드는 **수집 여부와 무관하게** 브리프로 간다. 원본 문서 지름길(`원본 ↗`)을 카드 안에
  남겨 2클릭 도달을 유지한다(절대원칙 1).
- 브리프가 없는 증거는 종전대로 동작한다. 분기 기준은 `hasBrief()` 한 곳뿐이라, 데이터가
  붙는 대로 카드·팝업이 자동 전환된다.

**고친 버그 3건**
1. 해금 토스트가 엔딩을 건너뛰고 `/notebook`으로 직행했다. 마지막 클루 대사가 재생되지 않고
   **E7 수집(7/7)도 일어나지 않았다**(엔딩이 그 둘을 담당한다). 누르는 시점에 목적지를 다시
   판단하도록 고쳤다.
2. 흐름 진행을 **애니메이션 완료(`onfinish`)에 걸어두면 탭이 가려졌을 때 영영 멈춘다.**
   WAAPI가 멈추기 때문이다 — 함정 2와 같은 부류. 상태는 즉시 바꾸고 움직임은 장식으로만 얹는다.
3. 단계마다 `setTimeout`을 새로 거는 **사슬 방식은 한 칸만 끊겨도 멈춘다.** 타이머 하나가
   매 틱 종료 조건을 직접 확인하는 방식으로 바꿨다.

**스탠딩 호흡** (`bbda7da`) — 정지 이미지가 굳어 보여서 4.2초 주기로 2px 위아래로만 움직인다.
표정이 바뀔 때의 반응 동작(튀어오르기 등)은 **시도했다가 뺐다** — 읽기를 방해한다.

**시도했다 되돌린 것 — 자세 전환 스프라이트**
스탠딩 4종은 표정만이 아니라 **자세가 다르다**(normal=가슴, happy·surprised=머리 위,
serious=턱에 손). 그래서 키가 바뀌면 팔이 순간이동한다. 중간 프레임 4장을 만들어 넣었으나
**사용자 판단으로 전부 제거**했다. 되살리지 말 것. 판단이 바뀐다면 필요한 동작은 두 개뿐이다
(가슴↔머리 위, 턱↔머리 위 — 얼굴 높이를 지나면 경로가 합쳐진다).

**데이터 관련 메모**
- 새 xlsx는 정규화된 스키마다(마스터/디테일 분리, FK 체인, `{param}` 치환자, Enum 상수).
- **`나기사(수영복)`가 `Student_Master`에 없다.** 역기획서 p13·p14 표에는 있다. 그래서 브리프
  편성은 엑셀에 실재하는 5인 + 우이로 짰다. 나기사를 살리려면 엑셀에 행을 추가해야 한다.
- 우이는 예외 4(EX 코스트 감소)를 발동시킨다. 감소량은 **소수점 절삭** — Skill_String에 명시돼
  있고 문서 p15 예시(`3 → 2`)와 계산이 맞는다.

---

### 8/26 — 플레이 기록 링크 추가

미니맵 공개 라이브러리(274개, 플레이 시간순)를 두 곳에 붙였다. 단순 취미 목록이 아니라
**자소서·수첩의 진술을 뒷받침하는 물증**이라서 넣었다 — 블루 아카이브 237h는 E2 역기획서,
스타세이비어 161h는 E5 기획서, 페르소나 5는 수첩 2부 '인생 게임'과 그대로 맞물린다.

- **기본 사항** — `보유 기술` 아래 새 섹션. 누구나 2클릭 안에 닿는다.
  데이터는 `data/resume.json`의 `playRecord` (비우면 렌더링 안 함).
- **수첩 2부 後** — 종합 소견 *"주장에는 반드시 물증을 붙이는 사람이다"* 바로 뒤.
  데이터는 `콘텐츠_수사수첩.json`의 `part2.evidence`. 내지라서 **독백체**로 썼다
  (독자 지칭·존댓말 금지 — 작업지시_대사톤개편 3장).

정렬은 사용자 지시로 `sort=PLAYTIME`. 맨 위가 이터널 리턴 760h인 점은 알고 둔 것이다.

**같이 고친 것 — pre-commit 훅의 한글 파일명 버그.** `core.quotepath`가 켜져 있으면 git이
한글 파일명을 `"ì..."` 로 이스케이프해 내보낸다. 그래서 훅이 `작업진행.md`가
스테이지에 있어도 못 찾아 막았고, `콘텐츠_*`·`스크립트_*` 변경도 감지하지 못했다.
`git -c core.quotepath=false` 로 고정했다. **이 저장소는 문서도 콘텐츠도 한글 파일명이라,
파일명을 다루는 스크립트를 새로 쓸 때마다 같은 함정이 있다.**

---

### 8/27 ~ 8/28 — E2 보완 + E1 브리프 에셋 준비

**E2 — 변한 것을 보이게 했다.** 플로우를 따라가는 동안 테이블 값이 조용히 바뀌어
어느 칸이 움직였는지 몰랐다. 바뀐 칸은 `이전 → 현재`를 1.5초간 보여준 뒤 값만 남는다.
지나온 노드에는 ✓가 붙고, 추적 중에는 관계없는 패널이 흐려진다.
큐 순번 열은 사용자 지시로 diff에서 뺀다(`NO_DIFF`) — 매 단계 바뀌는 값이라 강조해도 정보가 없다.

**E1 에셋 26장을 저장소에 넣었다.** 코드는 아직 없다 — 설계는 목업으로 확정했고
그때 쓸 그림만 먼저 넣는다. 한글 이름 원본 PNG는 전부 제외하고 가공본 `e1_*.webp`만 넣는다.

**한번 잃어버렸다.** 작업 중 `assets/img/` 안에 저장소 전체가 통째로 복사돼 있었고
(76MB · 412파일, `.git`은 `refs/`가 없는 절단된 사본), 그 과정에서 webp 두 개가 사라졌다.
전수 해시 비교로 사본에 고유한 파일이 0개임을 확인한 뒤 지웠고, 사라진 둘은 원본 PNG에서 다시 만들었다.
**이미지를 손대는 작업 뒤에는 `git status`로 사라진 파일을 한 번 확인한다.**

---

### 8/28 — E1 브리프 구현 + E2 관문 제거

**E1 「캐릭터 호출」을 붙였다** (`briefs/character-call.js`). 껍데기와 동선은 E2 것을 그대로 쓰고
`PLAYS`에 `kind`만 늘렸다. 다섯 장을 스크롤로 지난다 — 37명을 늘어놓고(역설),
한 줄짜리 카드를 초상에 끌어다 맞히고(첫인상), 내려갈수록 그 한 줄이 명찰이 되어 칸에 내려앉고(호출),
"소모되는 것 아닐까" 하고 되감아 다시 내려가면 이번엔 남은 것이 붙는다(결과).

**글자는 한 줄도 코드에 없다.** 줄바꿈은 `<br>` 대신 배열로 받는다. 캐릭터 색까지 데이터가
`--cc-ink`/`--cc-tint`로 심어 줘서 **CSS는 카라네·모모하·미미미라는 이름을 모른다** — 인물을 늘려도 CSS는 그대로다.

**함정 두 개를 다시 밟았다.** ① 화살표 `const`는 TDZ에 걸려 호이스팅된 `build()`보다 뒤에 있으면
`Cannot access before initialization`이 난다 — 헬퍼는 선언문으로 둔다.
② 날아가는 복제를 `position:fixed`로 두면 비행 중(0.58초) 스크롤한 만큼 착지점이 어긋난다.
슬롯은 문서에 고정돼 있으니 복제도 문서 좌표(`pageYOffset` 보정 + `absolute`)여야 정확히 겹친다.

**E2 — 원본 문서 앞의 관문을 없앴다.** 네 번을 눌러야 결론이 열리는 구조는 검토자에게 통행료를
물리는 셈이다. 마운트 때 바로 연다(`showRecap({scroll:false})` — 화면을 결론으로 끌어내리지 않는다).
`다시 해보기`를 눌러도 닫지 않는다.

**E2 — `EX_스킬_시전_가능_여부`가 굳어 있었다.** `paintTables()`는 흐름을 밟는 순간에만 도는데
그 사이 Cost는 계속 회복된다. 손패 카드만 살아나고 표는 직전 값에 굳어 있어서 이 열이 무엇을
보는 값인지 드러나지 않았다. `paint()`에서 그 칸만 직접 고쳐 쓴다 —
`paintTables()`를 부르면 100ms마다 전 칸이 다시 그려져 변화 강조가 계속 터진다.

---

### 8/28 — 원본 문서로 가는 길 정리 + E1 도입 교체

**원본까지 몇 번을 눌러야 하는지 실제로 세어 봤다.** 보관함 카드의 `원본 ↗`은 이미 1클릭인데,
브리프에 들어가면 결론까지 내려가야만 원본이 나온다 — E1은 다섯 장을 다 열면 8,000px가 넘는다.
그리고 문서 6개를 나란히 모아 둔 `/docs`는 **사이트 어디에서도 링크되지 않고 있었다**(`/making`과 같은 상태).

- **브리프 머리말에 `원본 문서 ↗`를 상시 둔다.** 어디까지 봤든 화면 위쪽에서 바로 열린다.
  인터랙션은 이해를 돕는 것이지 문서 앞의 통행료가 아니다. E1·E2 공통.
- **증거 보관함에 `문서만 모아보기 →`.** `/docs`를 되살린다. 브리프를 아예 건너뛸 검토자의 지름길.

**E1 도입을 세로 표지에서 가로 군상으로 바꿨다.** 620×876 표지를 제목 옆에 세우니 높이가 안 맞아
한쪽에 그림자만 한 여백이 남았다. 14–16권을 이어붙인 군상(1440×683)을 제목 아래 폭 전체로 놓으면
'100명'이 숫자가 아니라 그림으로 먼저 읽힌다. `e1_keyvisual.webp`는 이걸로 안 쓰게 됐다.

**장 꼬리말을 지웠다.** 머리말과 같은 문구를 장마다 아래에도 반복하고 있었다 — 종이 지면의 습관이지
스크롤에서는 구분선만 늘어난다.

**E1은 결론을 마지막에 연다.** E2와 반대다. E2는 조작이 곧 설명이라 결론이 먼저 보여도 손해가 없지만,
E1은 "소모되는 것 아닐까 → 아니었다"가 서사라서 결론이 1장 밑에 먼저 보이면 답을 먼저 읽는 셈이 된다.
마지막 장이 열리는 순간(`적용해본다면?`) 결론을 연다. 원본은 머리말에 있으니 갇히지는 않는다.

---

### 8/28 — 안 쓰는 에셋 정리

배포되는 자산 전부를 코드·데이터에서 역참조해 훑었다. **미참조 8개, 296KB를 지웠다.**

| 파일 | 왜 남았나 |
|---|---|
| `e1_keyvisual` (144KB) | 도입을 세로 표지에서 가로 군상으로 바꾸며 빠졌다 |
| `e1_karane_gain_bff` | 31화 매력이 `절친` → `믿음직한 선배`로 바뀌며 교체됐다 |
| `e1_karane_call·gyaru·combo`, `e1_momoha_call·luck·pity` | 원본 PDF에서 뽑았던 초기 컷. 실제 만화 컷으로 전부 대체됐다 |

**참조 검사에서 `.md`는 빼야 한다.** 처음엔 문서까지 훑었더니 `작업진행.md`의
"`e1_keyvisual.webp`는 이걸로 안 쓰게 됐다"는 문장이 참조로 잡혀 **안 쓴다고 적어 둔 파일이
쓰이는 것으로 나왔다.** 서술 문서는 사라진 것도 이름으로 부른다 — 역참조는 코드와 데이터만 본다.

`design/`은 13.5MB지만 `.vercelignore`에 있어 배포되지 않는다. 썸네일·폰트 원본이라 그대로 둔다.
CSS는 `.cc-*` 전부가 JS에서 쓰이는 것을 확인했다 — 죽은 규칙 없음.

**같이 고친 것 — E1 레일이 사이트 메뉴를 덮고 있었다.** `#site-header`가 `sticky; top:0; z-index:50`인데
`.cc-rail`도 `top:0`에 `z-index:60`이라, 스크롤하면 레일이 기본 사항·세부 사항·수집된 증거를 통째로 가렸다.
`top: var(--header-h)`로 머리말 아래에 붙이고 스택도 40으로 낮췄다.
**이 저장소에서 무언가를 화면 위에 고정할 때는 `--header-h`를 먼저 떠올린다** —
같은 처리가 이미 349행에 있었다(`top: calc(var(--header-h) + 20px)`).

---

### 8/28 — E1 퀴즈 2단 배치

초상 셋이 `max-width:1000px`에 묶여 1,385px 지면의 왼쪽만 쓰고 오른쪽이 비어 있었다.
왼쪽에 `Quiz / 한번 맞혀보세요! / 셋 다 맞히면 다음으로 넘어갑니다.`와 진행 표시를 세우고,
퀴즈는 오른쪽으로 몰아 폭을 다 쓴다(1,069px). 초상도 307 → 317px로 커졌다.
왼쪽 칸은 sticky라 카드를 끌어다 놓는 동안 계속 따라온다.

**진행 표시(`0 / 3`)를 넣은 이유** — 드래그 퀴즈는 몇 개를 더 맞혀야 끝나는지가 화면에 없었다.
맞힐 때마다 숫자가 올라가니 "이 칸이 언제 끝나는가"를 묻지 않게 된다.

**배포 확인을 잘못하고 있었다.** 지운 파일이 계속 200을 돌려주길래 배포가 안 된 줄 알았는데,
`vercel.json`의 SPA fallback 때문에 **없는 경로도 index.html을 200으로 돌려준다.**
`Content-Type`으로 봐야 한다 — 지워진 것은 `text/html` 2,312바이트, 살아 있는 것은 `image/webp`.
**이 저장소에서 파일 존재를 상태 코드로 확인하면 안 된다.**

---

### 8/28 — 첫 진입에 작업 중 양해 메모

정리가 도는 동안 방문한 사람에게 먼저 사정을 알린다. **`공사중` 배너는 쓰지 않았다** —
첫 화면부터 컨셉이 깨진다. 대신 **클루가 사건 파일 표지에 붙여 둔 포스트잇**으로 만들었다.
새 에셋 없이 수첩(2부)의 `--nb-*` 팔레트와 교보 손글씨 폰트만 빌려 쓴다.

**표지보다 먼저 세운다.** 오프닝이 돌기 시작하면 0.8초 뒤 대사가 이어져 읽을 틈이 없다.
그래서 메모를 넘겨야 `begin()`이 돌고, 그때부터 표지·대사가 예전과 똑같이 진행된다.
읽는 동안 `SKIP ≫`은 감춘다 — 실수로 건너뛰면 알릴 방법이 없다.

- 넘기기: 버튼 · `Enter` · `Esc` · `Space`. 버튼에 포커스가 자동으로 간다
- 세션 안에서는 다시 안 뜨고, **새로고침하면 다시 뜬다** (게임 상태에 `localStorage` 금지)
- `prefers-reduced-motion`이어도 메모는 먼저 뜬다 — 게이트가 `begin()` 바깥에 있다

**지울 때 코드를 안 건드린다.** `스크립트_인트로.json`의 `notice` 블록만 지우면 메모 단계가
통째로 사라지고 예전 흐름이 된다. 문구도 그 파일에서 고친다.

**한계 — `/` 로 들어올 때만 뜬다.** `/docs`나 증거 링크를 직접 받은 사람에게는 안 보인다.
전부 덮으려면 헤더 아래 상시 띠라야 하는데, 그건 모든 화면에서 컨셉을 계속 건드린다.

---

### 9/07 — 인터랙티브 브리프 모션·데이터 흐름 개선

공통 motion layer를 `assets/js/motion/`에 분리했다. Evidence → Detail → Brief는 View Transition으로
이미지와 제목을 잇고, 미지원 환경에서는 짧은 fade로 돌아간다. 뒤로가기 스크롤 위치를 복원하며,
빠르게 경로를 연속 선택해도 이전 transition이 최종 화면을 덮지 않는다.

E2의 플로우는 HTML 노드 13개와 기존 표의 정보량을 유지한 채 연결 관계를 SVG path로 바꿨다.
클릭 순간 Cost·손패·덱·예외 상태를 즉시 계산하고, 이후 모션은 확정된 snapshot만 순서대로 보여준다.
새 선택이나 탭 비활성화가 연출을 끊어도 최종 상태가 보존된다. READ → WRITE 타이포그래피로
Master Data와 Runtime State의 역할 차이를 먼저 보여준다.

E1은 정답 슬롯 주변의 약한 자석 반응, 정답 snap, 오답 복귀와 실제 목적지 DOM의 FLIP 이동을 넣었다.
드래그 없이 한 줄과 캐릭터를 차례로 선택하는 키보드 경로와 퀴즈 건너뛰기도 추가했다. 되감기는
650ms 안에 끝나고 입력 시 즉시 멈추며, 두 번째 pass와 facet 누적을 레일·결과 화면에서 구분한다.

- 새 파일: `assets/js/motion/{animate,evidence-header,flip,flow-paths,reveal,transition}.js`
- 검사: `tests/brief-regression.cjs`, `tests/serve.cjs`, `tests/README.md`
- 검증 범위: 라우팅 중단·뒤로/앞으로, Cost 부족/소모, 덱 순환, Cost 감소, 회복 증가,
  탭 비활성화, 실제 pointer drag·취소, 키보드 매칭, E1 두 pass·재시작, reduced-motion,
  View Transition 미지원 fallback, 320·390·768·1180·1440px 가로 넘침
- 마지막 polish는 Canvas 대신 560ms evidence scan으로 제한했다. 연속 particle이나 상시 animation은 없다.

**새 원칙:** 애플리케이션 상태는 interaction handler에서 확정하고, motion layer는 snapshot을 표현한다.
모션 취소나 `animation.finished` 여부를 상태 진행 조건으로 쓰지 않는다.

---

## 6. 현재 상태

- `main` 브랜치, `origin/main`과 동기화됨. 마지막 커밋 `bbda7da` (2026-08-26).
- 사이트는 기능적으로 완성 상태다. 7/27에 "완결 지어도 될지 검증"을 거쳤고, 이후는 개선 작업이다.
- **브리프는 E2에만 있다.** 나머지 5종(E1·E3~E6)은 메모까지만 되어 있고, 카드를 누르면
  종전대로 요약 화면 또는 원본 문서로 간다.
- **`/making`은 고아 라우트다.** E7이 수첩으로 교체된 뒤 어디서도 연결되지 않지만, 라우트·렌더러·
  `figures.js`·`콘텐츠_제작기.md`가 모두 살아 있고 **부팅 시 원고를 계속 fetch한다.**
  정리(삭제)든 유지든 **사용자에게 물어보고 결정할 것** — 임의로 지우지 말 것.
- 루트에 빈 `S/` 디렉터리가 있다. 용도 불명이며 git에 추적되지 않는다.

### 남은 일 / 후보

- **E3~E6 브리프 제작.** 껍데기(`views/brief.js`)와 동선은 그대로 재사용하고
  `briefs/`에 `kind`별 모듈만 추가하면 된다. 데이터에 `brief`가 붙는 순간 자동으로 전환된다.
- E2 브리프에 **툴팁 조립 확장** — `Skill_String`의 치환자에 값이 꽂히는 장면은 넣었지만,
  `Enum_Codes`·`Summon_Master`까지 훑는 경로는 아직 없다.
- `/making` 처리 결정 (제거 / 되살림 / 그대로 두기). `/docs`는 8/28에 보관함에서 링크를 붙여 되살렸다.
- Vercel Analytics 데이터 확인 — 계측은 8/05부터 쌓이고 있다. 진입 경로·체류 시간·이탈 지점을 보고
  어느 페이지에서 검토자가 떨어지는지 판단할 수 있다.
- 디자인 토큰 정리(오래된 미완 항목).
- `main.js`의 디버그 핸들 `window.__pr` — 배포본에서 제거할지 결정.

---

## 7. 함정 — 반복해서 문제가 됐던 것들

1. **`<img>`의 `src`를 바꿔 표정을 교체하지 말 것.** 캐시에 있어도 디코딩 중 빈 프레임이 생긴다.
   4종을 겹쳐 두고 `opacity`만 전환하는 현재 구조를 유지한다.
2. **`requestAnimationFrame` 기반 연출 금지.** 백그라운드 탭에서 멈춘다 — `setTimeout`으로 통일했다.
3. **게임 상태에 `localStorage` 금지** (명세 요구). 세션 메모리만.
4. **텍스트 하드코딩 금지.** 전부 데이터 파일에서 읽는다. `/docs`도 증거 카드 JSON을 복제하지 않고 참조한다.
5. **`resume.json`의 빈 값을 추측으로 채우지 말 것.** 비면 렌더링되지 않는 것이 의도다.
6. **커스텀 이벤트 값은 전부 문자열.** 같은 키에 숫자와 문자열이 섞이면 Vercel 대시보드 목록이 갈라진다.
   연속값(초)을 그대로 넣지 말고 버킷으로 변환한다.
7. **수첩 넘김 애니메이션은 reduced-motion에서도 재생한다.** 사용자 명시 지시이며, 일반적인 접근성
   관행과 반대다. 되돌리지 말 것.
8. **내부 문서를 새로 만들면 `.vercelignore`에 추가.** 안 그러면 배포된다.
9. **새 이미지·영상·폰트 에셋 추가는 기본적으로 지양.** 오프닝 트랜지션도 CSS만으로 구현했다.
10. 로컬에서 `/_vercel/insights/script.js`가 404인 것은 **정상**이다. Vercel이 배포 환경에서만 서빙한다.
11. **상태 변경을 애니메이션 완료에 걸지 말 것.** 탭이 가려지면 WAAPI가 멈춰 `onfinish`가 오지
    않는다. 화면과 상태를 먼저 맞추고 움직임은 위에 얹는다. 정리도 `setTimeout`이 한다.
12. **단계 진행을 setTimeout 사슬로 만들지 말 것.** 한 칸만 끊겨도 영영 멈춘다.
    타이머 하나가 매 틱 종료 조건을 확인하는 방식으로 짠다.
13. **경과 시간은 틱 간격을 가정하지 말고 `Date.now()` 차이로 잰다.** 가려진 탭에서 타이머가
    초당 1회로 묶이거나 아예 멈추기 때문이다. 가려진 동안은 아예 누적하지 않는 편이 낫다.
14. **미리보기 패널이 숨겨져 있으면 크롬이 그 탭의 타이머·부드러운 스크롤·WAAPI를 얼린다.**
    체감 확인이 불가능하니, 그런 항목은 사용자에게 직접 봐달라고 부탁할 것.

---

## 8. 작업 규칙 (사용자 선호)

- **커밋과 배포는 사용자 승인 후에만.** 확인 요청 → "좋다, 커밋하고 배포해줘"를 받고 진행하는 것이
  이 프로젝트의 패턴이다. 작업만 하고 보고한 뒤 대기할 것.
- **커밋마다 이 문서의 5장에 항목을 추가하고 같이 스테이지에 올린다.** `.githooks/pre-commit`이
  빠진 커밋을 막는다. 훅은 `core.hooksPath=.githooks`로 연결되어 있고, 새로 클론했다면
  `git config core.hooksPath .githooks`를 한 번 실행해야 한다.
- 작업 지시는 보통 **완료 판정 체크리스트가 붙은 .md 형식**으로 온다. 그 체크리스트를 그대로 검증하고
  결과를 보고한다.
- 변경 후에는 **회귀 확인**을 함께 보고한다(인트로 대사 진행 / 수집 / 수첩 넘김 / 라우팅 / 375px 모바일).
- **웹사이트에 직접 드러나지 않는 것은 고치지 않는다.** 드러나는 것 중 모순이 있으면 먼저 알리고 고친다.
- 사실을 지어내지 않는다. 이력·경력·수치는 사용자가 준 값만 쓴다.

---

## 9. 이전 세션 기록 위치

원본 트랜스크립트는 앱 목록에서 삭제되어도 파일은 남는다. 백업해 둔 위치:

```
C:\Users\gyummini\.claude\projects\_backup_PR_web\
```

| 파일 | 범위 |
|---|---|
| `c41de12a-....jsonl` | 2026-07-07 ~ 07-27 |
| `baf604ed-....jsonl` | 2026-07-07 ~ 07-31 (위 내용을 포함하는 상위 집합) |
| `b5dec872-....jsonl` | 2026-08-02 ~ 08-04 |
| `memory/pr-web-project.md` | 2026-07-12 기준 프로젝트 메모 — **일부 내용은 이 문서로 대체됨** |

이 문서의 옛 이름은 `인수인계.md`였다 (2026-08-26에 `작업진행.md`로 변경).

특정 시점의 코드나 대화가 필요하면 위 파일을 직접 읽으면 된다.
2026-08-13 작업의 대화 기록은 존재하지 않는다 — 5장의 커밋 분석이 유일한 기록이다.
