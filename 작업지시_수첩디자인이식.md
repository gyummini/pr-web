# 작업 지시 — 히든 페이지 '클루의 수사 수첩' 디자인 이식

## 전제

- 참조 시안: `클루의_수사_수첩__1a_.html` (프로젝트에 `design/` 폴더로 복사해둠). Claude Design이 만든 번들 파일로, 실제 페이지 마크업과 스타일은 `<script type="__bundler/template">` 블록 안에 있다. **이 파일을 통째로 사용하지 말 것** — 스타일과 구조만 추출해 기존 코드베이스 방식(콘텐츠/코드 분리, 해시 라우팅)으로 재구현한다.
- 원고: `히든_클루의수사수첩_초안.md` 의 텍스트가 콘텐츠 소스. 시안에 들어 있는 텍스트가 원고와 다르면 **원고가 우선**한다. 단 하나의 예외 — **접힌 페이지(말미) 섹션은 원고를 폐기하고 시안의 문구("여기까지 읽으셨다면, 탐정님은 이미 이 사무소의 공범이에요…")를 그대로 사용**한다. 원고에 있던 분석서 각주는 의도적으로 제외된 것이므로 복원하지 말 것.
- 대상 라우트: `#/notebook`. 콘텐츠_증거카드.json의 E7이 이 라우트를 가리킨다.

## 1. 디자인 토큰 추출

시안에서 다음을 추출해 기존 스타일 시스템(CSS 변수)에 수첩 전용 네임스페이스로 추가한다 (예: `--nb-*`):

- 색상: 종이 베이스(#f8f4e9, #ddd7c8, #e6dfc9 계열), 잉크(#26241f, #3d3a32), 도장 레드(#b23a2f, #8f2c23), 포스트잇(#fdf3bd)
- 폰트: 제목·손글씨 = 'Gaegu', 본문 = 'Gowun Dodum' (Google Fonts, 한글 서브셋 + `font-display: swap`). **이 페이지에서만 로드** — 본편 페이지의 폰트 로딩에 영향 주지 않을 것.
- 컴포넌트 스타일: 포스트잇 메모 박스, 마스킹테이프(반투명 회전 + mix-blend-mode), 폴라로이드/스크랩 프레임, 접힌 모서리(border 트릭), '열람 금지' 도장.

## 2. 필수 수정 3건 (시안과 다르게 구현할 것)

1. **더미 링크 실제 연결.** ① 2부 에픽세븐 메모의 "증거 E4 참조" → 수집된 증거 페이지의 E4 카드로 내부 라우팅(또는 E4 팝업 오픈). ② 하단 "사건 파일로 돌아가기" → 수집된 증거 페이지(#/evidence 상당)로 이동.
2. **본편 연결 컬러.** "사건 파일로 돌아가기" 버튼과 "증거 E4 참조" 링크에는 수첩 팔레트 대신 **본편의 네이비 포인트 컬러**를 사용한다. 본편으로 통하는 요소만 본편 색을 입는다는 규칙.
3. **이미지 슬롯 구분.** 시안은 이미지 3개가 전부 클루 SD로 채워져 있다. 실제 구현은: 1부 프로필 = 클루 SD 1개, 3부 현장 사진 = **별도 플레이스홀더 2슬롯** (assets/img/notebook_photo_1.png, notebook_photo_2.png — 파일이 없으면 "사진 준비 중" 스타일의 빈 폴라로이드 프레임으로 렌더). 사진은 지연 로딩.

## 3. 페이지 넘김 연출 (확정 요구사항)

수첩의 섹션들(표지 / 1부 / 2부 / 3부 / 접힌 페이지)이 한 화면에 나열되지 않고, 스크롤에 따라 **수첩 페이지가 한 장씩 넘어가는 경험**이 되도록 구현한다.

**반드시 지킬 것 (목표 수준의 요구):**
- 사용자의 스크롤 통제권을 뺏지 않는다. 스크롤 속도·방향이 사용자 입력대로 동작해야 하며, 답답함이 느껴지는 강제 이동은 금지.
- 2부(사건 외 기록 5항목)는 한 페이지에 다 들어가지 않으면 2~3항목씩 복수 페이지로 분할한다.
- 모바일에서는 연출 강도를 낮추거나 일반 스크롤로 폴백하여, 터치 스크롤과 충돌하지 않게 한다.
- `prefers-reduced-motion` 설정 사용자에게는 넘김 애니메이션을 생략한다.
- 현재 위치를 알 수 있는 페이지 인디케이터(예: 3/6)를 표시한다.

**구현 방식 (추천이며 강제 아님):**
- 1안(추천): CSS scroll-snap으로 섹션 경계에 스냅을 걸고, 스냅 시점에 가벼운 종이 넘김 애니메이션을 얹는 방식. 네이티브 스크롤을 유지하므로 위 요구를 만족시키기 가장 쉽다.
- 2안: 클릭/키보드 기반 완전 페이지네이션(책 방식). 채택 시 페이지 인디케이터와 이전/다음 버튼 필수.
- 휠 이벤트를 가로채는 스크롤 하이재킹 방식은 트랙패드·모바일 오작동 위험이 크므로 권장하지 않는다. 다른 방식을 쓰더라도 위 '반드시 지킬 것'을 만족하면 허용.

## 4. 유지할 것 (변경 금지)

- 기존 수집 로직, 히든 해금 조건(E1~E6 수집), 엔딩 대화 → 수첩 진입 흐름은 그대로.
- 수첩 텍스트는 데이터 파일로 분리(기존 콘텐츠 관리 방식과 동일). 하드코딩 금지.
- '수사 결과만 본다' 경로에서도 E7 카드 클릭 시 이 페이지에 접근 가능해야 한다(엔딩 대화 없이 직행).
- 본편 페이지들의 스타일에는 어떤 변화도 없어야 한다.

## 5. 완료 판정 체크리스트

- [ ] #/notebook 진입 시 수첩 톤(종이 질감, 손글씨 제목) 렌더링, 본문은 고운돋움으로 가독 확보
- [ ] 스크롤 시 섹션이 페이지 넘김으로 전환되며, 스크롤 통제권이 자연스러움 (트랙패드·마우스 휠 모두 확인)
- [ ] 페이지 인디케이터 표시, 2부 분할 페이지 정상
- [ ] 모바일에서 넘김 연출이 터치 스크롤과 충돌하지 않음 (폴백 동작 확인)
- [ ] prefers-reduced-motion 환경에서 애니메이션 생략 확인
- [ ] 전 섹션(표지/1부/2부 5항목/3부/접힌 페이지) 존재, 텍스트가 원고와 일치 (접힌 페이지만 시안 문구)
- [ ] E4 참조 링크와 돌아가기 버튼이 실제 라우트로 동작하고 네이비 컬러
- [ ] 현장 사진 슬롯 2개가 SD와 별개로 존재 (파일 부재 시 빈 프레임 처리)
- [ ] 폰트 2종이 이 페이지에서만 로드되고 본편 성능에 영향 없음
- [ ] 엔딩 대화 → 수첩 진입, 증거 목록 → 수첩 진입 두 경로 모두 정상
- [ ] 모바일에서 레이아웃 깨짐 없음 (테이프·폴라로이드 겹침 확인)
- [ ] 기존 기능 회귀 없음: 수집 카운트, 팝업, 챕터 이탈 일괄 수집 정상
